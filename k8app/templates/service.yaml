apiVersion: v1 
kind: Service 
metadata: 
  name: frontend 
spec: 
  selector: 
    app: frontend 
  type: {{ .Values.service.type }}
  ports: 
    - name: http 
      port: {{ .Values.service.port }} 
      targetPort: 8080